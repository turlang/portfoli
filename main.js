const menuBtn=document.querySelector('.menu-button');const nav=document.querySelector('.nav');menuBtn?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuBtn.setAttribute('aria-expanded',String(open));});

const cards=[...document.querySelectorAll('.project-card')];let current=0;function render(){const total=cards.length;cards.forEach((card,i)=>{let offset=i-current;if(offset>total/2)offset-=total;if(offset<-total/2)offset+=total;const abs=Math.abs(offset);const x=offset*300;const z=-abs*150;const ry=offset*-19;const scale=1-abs*.09;card.classList.toggle('active',offset===0);card.style.opacity=abs>2?0:.35+(1-abs/3)*.65;card.style.zIndex=String(20-abs);card.style.transform=`translateX(calc(-50% + ${x}px)) translateZ(${z}px) rotateY(${ry}deg) scale(${scale})`;});}
function next(){current=(current+1)%cards.length;render()}function prev(){current=(current-1+cards.length)%cards.length;render()}document.querySelector('.next')?.addEventListener('click',next);document.querySelector('.prev')?.addEventListener('click',prev);cards.forEach((c,i)=>c.addEventListener('click',()=>{current=i;render()}));render();let timer=setInterval(next,4200);document.querySelector('.carousel-shell')?.addEventListener('mouseenter',()=>clearInterval(timer));document.querySelector('.carousel-shell')?.addEventListener('mouseleave',()=>timer=setInterval(next,4200));

const tilt=document.querySelector('[data-tilt]');document.addEventListener('mousemove',e=>{if(!tilt||innerWidth<800)return;const x=(e.clientX/innerWidth-.5)*10;const y=(e.clientY/innerHeight-.5)*-8;tilt.style.transform=`rotateX(${9+y}deg) rotateY(${-16+x}deg)`;});

const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('in-view')})},{threshold:.12});document.querySelectorAll('.content-section,.paper-block,.contact-section,.hero-copy,.hero-device').forEach(el=>observer.observe(el));
