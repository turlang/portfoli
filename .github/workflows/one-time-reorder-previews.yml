name: Ajuste concluído — arquivo inativo

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - run: echo "A prioridade das imagens já foi atualizada diretamente na main."
